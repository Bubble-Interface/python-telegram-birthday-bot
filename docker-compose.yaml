version: '3'
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: counter
    ports:
      - '5432:5432'
    volumes:
      - birthday_bot_db_data:/var/lib/postgresql/data

  # bot:
  #   build:
  #     context: .
  #   restart: always
  #   depends_on:
  #     - postgres
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: counter
  #     TOKEN: $TOKEN

volumes:
  birthday_bot_db_data:
